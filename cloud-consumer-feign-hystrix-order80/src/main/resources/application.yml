server:
  port: 80

eureka:
  client:
    register-with-eureka: false #表示是否将自己注册进eurekaServer默认为true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
feign:
  hystrix:
    enabled: true